# Future ideas
Not to be acted on without prior approval

## Config and arguments
Do need config options soon, but make it simple.  
What is the go argparse package which allows an environment var override, `FOOVAR=xyz <program to run>` same as `<program to run> --foovar=xyz`?  
Config file in .config/<program name>/ and does it allow dropins too?

